# Architecture

Frontend: React + TypeScript + Vite.
Backend: Supabase (Postgres, Auth, Edge Functions, Storage).
Deploy via Vercel. Keep UI components reusable and business logic separated from presentation.